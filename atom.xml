<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>undefined&#39;s Blog</title>
  
  <subtitle>1</subtitle>
  <link href="https://undefined.today/atom.xml" rel="self"/>
  
  <link href="https://undefined.today/"/>
  <updated>2025-06-13T02:12:35.606Z</updated>
  <id>https://undefined.today/</id>
  
  <author>
    <name>Michael</name>
    
  </author>
  
  <generator uri="https://hexo.io/">Hexo</generator>
  
  <entry>
    <title>并发模型的底层实现</title>
    <link href="https://undefined.today/posts/1745d1c/"/>
    <id>https://undefined.today/posts/1745d1c/</id>
    <published>2025-06-13T02:09:55.000Z</published>
    <updated>2025-06-13T02:12:35.606Z</updated>
    
    <content type="html"><![CDATA[<h1 id="CSP（通信顺序进程）模型的核心思想"><a href="#CSP（通信顺序进程）模型的核心思想" class="headerlink" title="CSP（通信顺序进程）模型的核心思想"></a>CSP（通信顺序进程）模型的核心思想</h1><p>CSP模型的核心思想是 <strong>“不要通过共享内存来通信，而是通过通信来共享内存”</strong>  。这一思想颠覆了传统并发编程中依赖锁机制保护共享内存的方式，转而通过<strong>消息传递</strong> （Message Passing）实现协程间的安全协作。其实上本质是把数据所有权通过Channel在goroutine间显式传递，而非隐式地共享内存地址空间。</p><h2 id="三个模块"><a href="#三个模块" class="headerlink" title="三个模块"></a>三个模块</h2><h3 id="goroutine"><a href="#goroutine" class="headerlink" title="goroutine"></a>goroutine</h3><p>Go运行时管理的用户态协程，创建成本仅为2KB栈内存（详细的内容可以去看看前面两个博文），竟然还支持百万级并发。与操作系统线程的1MB默认栈空间相比，他的轻量化特性让CSP模型在Go中具备可行性。</p><h3 id="channel"><a href="#channel" class="headerlink" title="channel"></a>channel</h3><p>Channel作为goroutine间通信的唯一媒介，具有以下关键特性：</p><ul><li><strong>类型安全</strong> ：通道必须声明传递的数据类型</li><li><strong>同步机制</strong> ：无缓冲通道（unbuffered channel）自动实现发送&#x2F;接收操作的同步</li><li><strong>所有权转移</strong> ：通过通道传递数据时，发送方自动放弃数据所有权（这个可能不是很好理解，就当作离婚的时候男生净身出户）</li><li><strong>多路复用</strong> ：<code>select</code>语句支持多通道监听，实现事件驱动的并发模型</li></ul><h3 id="范式"><a href="#范式" class="headerlink" title="范式"></a><strong>范式</strong></h3><p>Go通过<code>go</code>关键字启动协程，配合Channel的阻塞特性，天然支持以下模式：</p><ul><li>Worker Pool</li><li>Pipeline</li><li>Context cancellation（这个我在前面的两篇文章简单的说到了）</li></ul><h2 id="和传统并发模型的对比"><a href="#和传统并发模型的对比" class="headerlink" title="和传统并发模型的对比"></a>和传统并发模型的对比</h2><table><thead><tr><th>特点</th><th>CSP</th><th>JAVA的共享内存</th></tr></thead><tbody><tr><td>通信方式</td><td>Channel</td><td>共享变量+原子操作或者是上锁</td></tr><tr><td>数据竞争风险</td><td>0（所有权转移保障）</td><td>高（需要手动进行同步）</td></tr><tr><td>死锁检测</td><td>可通过<code>go vet</code>静态分析</td><td>要用第三方工具</td></tr><tr><td>并发复杂度</td><td>O(n)（线性可组合）</td><td>O(n²)（锁竞争指数增长）</td></tr><tr><td>调度开销</td><td>协程切换&lt;100ns</td><td>线程切换~1μs</td></tr></tbody></table><blockquote><p>扯了这么多，现在我们来上底层代码来看看这到底是怎么一回事。</p></blockquote><h1 id="goroutine-的实现原理和机制"><a href="#goroutine-的实现原理和机制" class="headerlink" title="goroutine 的实现原理和机制"></a>goroutine 的实现原理和机制</h1><h2 id="轻量级线程的实现原理"><a href="#轻量级线程的实现原理" class="headerlink" title="轻量级线程的实现原理"></a><strong>轻量级线程的实现原理</strong></h2><p>Goroutine是Go语言实现的用户级协程（User-Level Coroutine），核心优势在于<strong>极低的资源消耗与高效的调度能力</strong> 。每个Goroutine的初始堆栈大小仅为2KB（x86架构下），远小于传统线程的2MB默认值。这种设计通过以下机制实现：</p><h3 id="动态堆栈调整"><a href="#动态堆栈调整" class="headerlink" title="动态堆栈调整"></a><strong>动态堆栈调整</strong></h3><p>Go运行时（Runtime）通过<strong>基于信号的栈溢出检测</strong>的技术，动态扩容或收容Goroutine的堆栈空间。当函数调用需要更多栈空间时，运行时会分配新内存块并复制原有栈数据，确保程序无需手动管理栈大小。这一过程由编译器在函数入口插入的<code>morestack</code>检查逻辑触发</p><blockquote><p>详见： <a href="https://undefined.today/posts/cc4c72ab/">https://undefined.today/posts/cc4c72ab/</a></p></blockquote><h3 id="内存占用优化"><a href="#内存占用优化" class="headerlink" title="内存占用优化"></a><strong>内存占用优化</strong></h3><p>Goroutine的控制结构（如调度信息、寄存器状态）仅占用约40B内存，而线程的控制块通常超过1KB。这种紧凑设计使得单机可轻松支持数十万并发Goroutine，而且，运行时采用的是<strong>空闲Goroutine缓存池</strong> （<code>sync.Pool</code>类似机制）复用已退出的Goroutine资源，降低频繁创建&#x2F;销毁的开销。</p><h3 id="M-N线程调度模型"><a href="#M-N线程调度模型" class="headerlink" title="M:N线程调度模型"></a>M:N线程调度模型</h3><p>Go调度器用的是M:N调度模型*，也就是把M个Goroutine（G）映射到N个操作系统的线程（P）上执行，形成多对多的关联关系，核心组件包括：</p><ul><li><strong>G（Goroutine）</strong> ：用户代码的执行单元，包含栈信息、状态机等。</li><li><strong>M（Machine）</strong> ：操作系统线程，负责执行Goroutine。</li><li><strong>P（Processor）</strong> ：逻辑处理器，持有调度器所需的运行队列（Run Queue）和资源锁</li></ul><p>调度的整个过程遵循这个规则：</p><ul><li><p>每个M必须绑定一个P才能运行Goroutine，P的数量通常等于CPU核心数（可通过<code>GOMAXPROCS</code>调整）</p></li><li><p>当Goroutine发生系统调用或阻塞时，M会与P解绑，释放P供其他M使用，避免线程阻塞导致全局调度停滞</p></li><li><p>本地运行队列（Local Run Queue）优先执行绑定P的G，全局队列（Global Run Queue）用于负载均衡</p></li></ul><p>这个调度模型通过减少线程切换次数（仅在M:N调度处罚上下文切换），很明显的降低了内核态与用户态的切换成本</p><h1 id="Goroutine创建与销毁的开销分析"><a href="#Goroutine创建与销毁的开销分析" class="headerlink" title="Goroutine创建与销毁的开销分析"></a>Goroutine创建与销毁的开销分析</h1><h2 id="创建开销"><a href="#创建开销" class="headerlink" title="创建开销"></a><strong>创建开销</strong></h2><p>Goroutine的创建由<code>go</code>关键字触发，运行时仅需分配少量内存（约2KB栈+控制结构），并通过原子操作将G（上面MN调度模型的概念）加入运行队列。相比之下，线程创建需向操作系统申请资源，甚至是Goroutine的数百倍</p><h2 id="销毁开销"><a href="#销毁开销" class="headerlink" title="销毁开销"></a><strong>销毁开销</strong></h2><p>Goroutine退出时，运行时将其标记为“可回收”状态，并延迟释放资源以避免频繁内存分配。假设Goroutine泄漏（如未退出的死循环），垃圾回收器（GC）会检测并清理其占用的内存，这种自动管理机制避免了线程池的手工维护成本。</p><h2 id="Channel通信机制"><a href="#Channel通信机制" class="headerlink" title="Channel通信机制"></a>Channel通信机制</h2><h3 id="无缓冲与有缓冲Channel的底层数据结构"><a href="#无缓冲与有缓冲Channel的底层数据结构" class="headerlink" title="无缓冲与有缓冲Channel的底层数据结构"></a><strong>无缓冲与有缓冲Channel的底层数据结构</strong></h3><p>Channel的底层实现基于<strong>环形队列（Circular Buffer）</strong> ，其核心结构体<code>hchan</code>包含以下关键字段：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">type</span> hchan <span class="keyword">struct</span> &#123;</span><br><span class="line">    qcount   <span class="type">uint</span>           <span class="comment">// 当前元素数量</span></span><br><span class="line">    dataqsiz <span class="type">uint</span>           <span class="comment">// 缓冲区大小（0表示无缓冲）</span></span><br><span class="line">    buf      unsafe.Pointer <span class="comment">// 指向环形缓冲区的指针</span></span><br><span class="line">    elemsize <span class="type">uint16</span>         <span class="comment">// 元素大小</span></span><br><span class="line">    closed   <span class="type">uint32</span>         <span class="comment">// 关闭状态标志</span></span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><ul><li><p><strong>无缓冲Channel</strong> ：<code>dataqsiz=0</code>，发送与接收操作必须同步配对，发送方直接将数据拷贝到接收方栈空间</p></li><li><p><strong>有缓冲Channel</strong> ：<code>dataqsiz&gt;0</code>，环形缓冲区通过<code>sendx</code>和<code>recvx</code>索引追踪读写位置，实现异步通信</p></li></ul><h3 id="发送与接收操作的同步-异步实现"><a href="#发送与接收操作的同步-异步实现" class="headerlink" title="发送与接收操作的同步&#x2F;异步实现"></a><strong>发送与接收操作的同步&#x2F;异步实现</strong></h3><h4 id="同步操作（无缓冲Channel）"><a href="#同步操作（无缓冲Channel）" class="headerlink" title="同步操作（无缓冲Channel）"></a><strong>同步操作（无缓冲Channel）</strong></h4><p>发送方调用<code>chansend</code>后会进入休眠，知道有接收方通过<code>chanrecv</code>唤醒他。</p><p>接收方会先尝试从发送等待队列（<code>sendq</code>）获取数据，避免缓冲区拷贝开销。</p><h4 id="异步操作（有缓冲Channel）"><a href="#异步操作（有缓冲Channel）" class="headerlink" title="异步操作（有缓冲Channel）"></a><strong>异步操作（有缓冲Channel）</strong></h4><p>数据会先写入环形缓冲区，仅当缓冲区满时发送方才阻塞。</p><p>接收方会从缓冲区读取数据，缓冲区为空时接收方才阻塞。</p><blockquote><p>运行时通过互斥锁（<code>lock</code>字段）保护共享缓冲区，并利用<code>runtime.sema</code>实现发送&#x2F;接收方的等待队列管理</p></blockquote><h3 id="Channel的闭锁与多路复用（Select语句）"><a href="#Channel的闭锁与多路复用（Select语句）" class="headerlink" title="Channel的闭锁与多路复用（Select语句）"></a><strong>Channel的闭锁与多路复用（Select语句）</strong></h3><h4 id="Channel关闭机制"><a href="#Channel关闭机制" class="headerlink" title="Channel关闭机制"></a><strong>Channel关闭机制</strong></h4><p>调用<code>close(ch)</code>会设置<code>closed</code>标志位，并唤醒所有等待的接收方（返回零值）和发送方（触发panic）。关闭操作会通过原子写确保可见性，避免竞态条件。</p><h4 id="select的实现原理"><a href="#select的实现原理" class="headerlink" title="select的实现原理"></a>select的实现原理</h4><p><code>select</code>语句的底层通过<strong>随机化轮询</strong> 策略选择就绪的Channel：</p><ul><li>编译器将<code>select</code>转换为<code>runtime.selectgo</code>函数调用。</li><li>上面的那个函数会遍历所有Channel的发送&#x2F;接收状态，优先选择可立即完成的操作（如非空Channel或已关闭的Channel）。</li><li>假设存在多个就绪Case，那么就通过<code>fastrand</code>随机选择，避免出现饿死问题</li></ul><h1 id="调度器原理"><a href="#调度器原理" class="headerlink" title="调度器原理"></a>调度器原理</h1><p>Go调度器的核心是GMP模型，由 <strong>G（Goroutine）</strong> 、<strong>M（Machine，系统线程）</strong>  和 <strong>P（Processor，逻辑处理器）</strong>  三部分构成，其设计目标是实现高并发下的高效调度与负载均衡。</p><p><strong>G（Goroutine）</strong> ：用户态轻量级协程，存储自身执行栈、状态及上下文信息。每个G通过<code>g0</code>和<code>gsignal</code>栈实现系统调用和信号处理的切换</p><p><strong>M（Machine）</strong> ：对应操作系统线程，负责实际执行G代码。M通过绑定P获取可运行的G，并维护线程本地缓存（如内存分配的<code>mcache</code>）以减少锁竞争</p><p><strong>P（Processor）</strong> ：逻辑处理器，管理本地可运行G队列（LRQ），最大容量为256个G</p><blockquote><p>P还维护全局资源（如内存分配器、调度器状态），并通过<code>runq</code>队列调度G。P的数量由<code>GOMAXPROCS</code>参数决定，通常与CPU核心数一致</p></blockquote><h2 id="交互流程"><a href="#交互流程" class="headerlink" title="交互流程"></a><strong>交互流程</strong></h2><p><strong>绑定关系</strong> ：M必须绑定P才能运行G，形成“M-P-G”三级关联。当M因系统调用阻塞时，P会被释放并重新绑定其他M以继续执行G</p><p><strong>任务分配</strong> ：G创建后优先加入当前P的本地队列；若本地队列满，则进入全局队列（Global Run Queue, GRQ）</p><p><strong>调度循环</strong> ：M绑定P后，持续从P的<code>runq</code>中取出G执行，若队列为空则尝试从其他P的队列尾部“窃取”G（Work Stealing）或从GRQ中获取任务</p><h2 id="调度器的抢占式调度与协作式调度策略"><a href="#调度器的抢占式调度与协作式调度策略" class="headerlink" title="调度器的抢占式调度与协作式调度策略"></a><strong>调度器的抢占式调度与协作式调度策略</strong></h2><p>Go调度器早期采用<strong>协作式调度</strong> ，依赖G主动让出CPU（如通过<code>runtime.Gosched()</code>或系统调用触发调度）。但此模式存在“长任务饿死其他G”的风险。自Go 1.14起，调度器引入<strong>基于信号的抢占式调度</strong> ，通过内核信号（如<code>SIGURG</code>）强制中断长时间运行的G，确保公平性</p><h3 id="协作式调度触发场景"><a href="#协作式调度触发场景" class="headerlink" title="协作式调度触发场景"></a><strong>协作式调度触发场景</strong></h3><ul><li><p>G主动调用<code>Gosched()</code>让出CPU</p></li><li><p>G进入系统调用（如<code>syscall</code>）或同步阻塞（如<code>channel</code>操作），触发M与P的解绑，允许其他G运行</p></li></ul><h3 id="抢占式调度实现"><a href="#抢占式调度实现" class="headerlink" title="抢占式调度实现"></a><strong>抢占式调度实现</strong></h3><p><strong>时间片限制</strong> ：每个G的执行时间片由调度器隐式管理，若超过阈值（通常为10ms），则发送信号中断当前M。</p><p><strong>异步抢占</strong> ：M收到信号后，触发<code>mcall</code>跳转到调度循环，保存当前G上下文并重新选择下一个G执行</p><h2 id="工作窃取（Work-Stealing）与负载均衡机制"><a href="#工作窃取（Work-Stealing）与负载均衡机制" class="headerlink" title="工作窃取（Work Stealing）与负载均衡机制"></a><strong>工作窃取（Work Stealing）与负载均衡机制</strong></h2><p>为了解决多核场景下的负载不均问题，GMP模型引入名为<strong>工作窃取的算法</strong> ，结合本地队列与全局队列实现动态平衡：</p><ol><li><p><strong>本地队列优先</strong> ：每个P维护私有<code>runq</code>，减少锁的竞争。G入队&#x2F;出队仅需原子操作，效率高</p></li><li><p><strong>窃取逻辑</strong> ：假设P的本地队列为空时，按以下顺序尝试获取任务：</p></li></ol><ul><li>从全局队列（GRQ）中获取一批G（数量为<code>n = min(len(GRQ), GOMAXPROCS)</code>）。</li><li>随机选择一个其他P的队列尾部窃取二分之一的G（如从队列尾部取<code>n/2</code>个），避免头部竞争</li></ul><ol start="3"><li><strong>负载均衡触发时机</strong> ：</li></ol><ul><li>M绑定P后首次调度时主动窃取。</li><li>M空闲超过1毫秒时触发窃取</li></ul><h2 id="调度器的性能优化与多核CPU适配"><a href="#调度器的性能优化与多核CPU适配" class="headerlink" title="调度器的性能优化与多核CPU适配"></a><strong>调度器的性能优化与多核CPU适配</strong></h2><p>Go调度器针对多核架构进行了深度优化，核心策略有：</p><ol><li><p><strong>并行度控制</strong> ：</p><ul><li><p><code>GOMAXPROCS</code>限制最大并行度（默认为CPU核心数），避免线程过多导致上下文切换开销</p></li><li><p><strong>动态调整</strong>M数量：架设M因为系统调用阻塞时，调度器就回去创建新M（不超过<code>GOMAXPROCS</code>）维持并行度</p></li></ul></li><li><p><strong>减少锁竞争</strong> ：</p><ul><li><p><strong>本地资源隔离</strong> ：P去维护本地内存分配器（<code>mcache</code>）、G队列等资源，避免全局锁</p></li><li><p><strong>原子操作替代锁</strong> ：G的队列操作使用原子指令（如CAS），而不是非互斥锁，提升并发性能</p></li></ul></li><li><p><strong>NUMA亲和性优化</strong> ：</p><ul><li>M绑定P后，优先使用P关联的本地的内存的分配器，减少多NUMA节点访问延迟</li></ul></li><li><p><strong>系统调用优化</strong> ：</p><ul><li><p><strong>非阻塞I&#x2F;O支持</strong> ：网络I&#x2F;O通过<code>netpoller</code>异步处理，避免M阻塞</p></li><li><p><strong>快速系统调用切换</strong> ：进入系统调用前，M会先去释放P，然后就允许其他M绑定该P继续执行G</p></li></ul></li><li><p><strong>监控与反馈机制</strong> ：</p><ul><li><p><strong>Sysmon线程</strong> ：独立运行的监控线程，负责检测长任务、触发抢占、管理垃圾回收（GC）辅助工作</p></li><li><p><strong>性能统计</strong> ：通过<code>pprof</code>工具分析调度延迟、G阻塞次数等指标，辅助优化代码</p></li></ul></li></ol>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;CSP（通信顺序进程）模型的核心思想&quot;&gt;&lt;a href=&quot;#CSP（通信顺序进程）模型的核心思想&quot; class=&quot;headerlink&quot; title=&quot;CSP（通信顺序进程）模型的核心思想&quot;&gt;&lt;/a&gt;CSP（通信顺序进程）模型的核心思想&lt;/h1&gt;&lt;p&gt;CSP模型的</summary>
      
    
    
    
    
  </entry>
  
  <entry>
    <title>Go Context 控制并发的核心机制</title>
    <link href="https://undefined.today/posts/80a99c2d/"/>
    <id>https://undefined.today/posts/80a99c2d/</id>
    <published>2025-03-22T06:12:43.000Z</published>
    <updated>2025-06-12T11:53:07.942Z</updated>
    
    <content type="html"><![CDATA[<p>Go 的 <code>context</code> 包是并发编程的核心工具，他的设计通过统一接口简化了 goroutine 的生命周期管理。其实核心就在于 <code>context.Context</code> 接口的四个方法：<code>Deadline()</code>、<code>Done()</code>、<code>Err()</code> 和 <code>Value(key)</code>。但是呢 <code>cancelCtx</code> 和 <code>timerCtx</code> 是其实现的关键结构，分别用于处理主动取消和超时取消。</p><h1 id="取消流程"><a href="#取消流程" class="headerlink" title="取消流程"></a>取消流程</h1><p><code>cancelCtx</code> 是支持取消操作的核心结构，源码：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">type</span> cancelCtx <span class="keyword">struct</span> &#123;</span><br><span class="line">    Context</span><br><span class="line">    mu       sync.Mutex</span><br><span class="line">    done     atomic.Value</span><br><span class="line">    children <span class="keyword">map</span>[canceler]<span class="keyword">struct</span>&#123;&#125;</span><br><span class="line">    err      <span class="type">error</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="done-channel"><a href="#done-channel" class="headerlink" title="done channel"></a>done channel</h2><p>当调用 <code>cancel()</code> 方法的时候，他会首先会标记错误状态（比如说 <code>Canceled</code> 或 <code>DeadlineExceeded</code>），并通过 <code>closedchan</code> 去关闭 <code>done</code> channel。具体步骤如下：</p><ul><li><strong>原子操作保护</strong> ：使用互斥锁 <code>mu</code> 确保并发安全，防止多次调用 <code>cancel()</code>。</li><li><strong>错误状态标记</strong> ：将 <code>err</code> 字段设置为指定错误值（如 <code>Canceled</code>），后续调用 <code>Err()</code> 会返回该值。</li><li><strong>关闭 <code>done</code> channel</strong> ：通过 <code>closedchan</code>（一个已关闭的空结构体 channel）替换原 <code>done</code> channel，触发响应 。</li></ul><h2 id="取消子节点"><a href="#取消子节点" class="headerlink" title="取消子节点"></a><strong>取消子节点</strong></h2><p><code>cancelCtx</code> 还维护了一个子节点集合（<code>children map[canceler]struct&#123;&#125;</code>），当调用 <code>cancel()</code> 时会遍历所有子节点并<strong>递归</strong>调用它们的 <code>cancel()</code> 方法。简单来说只要执行了这个，那么就能确定取消信号能在父节点之间传播下去了：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">for</span> child := <span class="keyword">range</span> c.children &#123;</span><br><span class="line">    child.cancel(<span class="literal">false</span>, err, <span class="literal">false</span>)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><ul><li><p><strong>子节点类型</strong> ：子节点可能是 <code>cancelCtx</code> 或 <code>timerCtx</code>，均实现 <code>canceler</code> 接口。</p></li><li><p><strong>传播机制</strong> ：递归调用保证父节点取消时，所有子节点同步被取消</p></li></ul><h2 id="从父节点删除自己"><a href="#从父节点删除自己" class="headerlink" title="从父节点删除自己"></a><strong>从父节点删除自己</strong></h2><p>假设当前 <code>cancelCtx</code> 的父节点也是可取消的contetxt（如 <code>cancelCtx</code> 或 <code>timerCtx</code>），那么就去调用 <code>removeChild</code> 从父节点的子节点集合中把自己删掉，释放资源：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span> p, ok := c.Context.(*cancelCtx); ok &#123;</span><br><span class="line">    p.mu.Lock()</span><br><span class="line">    <span class="keyword">if</span> s, ok := p.children[c]; ok &#123;</span><br><span class="line">        <span class="built_in">delete</span>(p.children, c)</span><br><span class="line">    &#125;</span><br><span class="line">    p.mu.Unlock()</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="懒加载"><a href="#懒加载" class="headerlink" title="懒加载"></a>懒加载</h2><p><code>done</code> channel 用的是懒加载策略（通过 <code>atomic.Value</code> 存储），只会在首次调用 <code>Done()</code> 的时候去初始化。通过这个方式可以优化内存占用，但是还是需要在并发访问时确保原子性 。</p><h1 id="timerCtx-的超时处理"><a href="#timerCtx-的超时处理" class="headerlink" title="timerCtx 的超时处理"></a><code>timerCtx</code> 的超时处理</h1><p><code>timerCtx</code> 直接继承了 <code>cancelCtx</code>，通过增加一个定时器字段就可以实现超时取消：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">type</span> timerCtx <span class="keyword">struct</span> &#123;</span><br><span class="line">    cancelCtx</span><br><span class="line">    timer *time.Timer</span><br><span class="line">    deadline time.Time</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="WithDeadline"><a href="#WithDeadline" class="headerlink" title="WithDeadline"></a>WithDeadline</h2><p>当调用 <code>context.WithDeadline(parent Context, deadline time.Time)</code> 的时候，发生了：</p><ul><li><strong>计算剩余时间</strong> ：假设 <code>deadline</code> 过了，那么立即取消；否则计算剩余时间 <code>d := t.deadline.Sub(now)</code>。</li><li><strong>启动定时器</strong> ：创建一个 <code>time.Timer</code>，在 <code>d</code> 时间后触发 <code>cancel()</code> 方法：</li></ul><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">t.timer = time.AfterFunc(d, <span class="function"><span class="keyword">func</span><span class="params">()</span></span> &#123;</span><br><span class="line">    t.cancel(<span class="literal">true</span>, DeadlineExceeded, <span class="literal">false</span>)</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure><ul><li><strong>传播机制</strong> ：超时触发后，<code>timerCtx</code> 会调用 <code>cancel()</code>，继承 <code>cancelCtx</code> 的递归取消逻辑。</li></ul><h2 id="提前取消与资源释放"><a href="#提前取消与资源释放" class="headerlink" title="提前取消与资源释放"></a><strong>提前取消与资源释放</strong></h2><p>如果说定时器触发前就调用 <code>cancel()</code> 的话，就得手动停止定时器以避免资源泄漏：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(c *timerCtx)</span></span> cancel(removeFromParent <span class="type">bool</span>, err <span class="type">error</span>, manual <span class="type">bool</span>) &#123;</span><br><span class="line">    c.cancelCtx.cancel(removeFromParent, err, manual)</span><br><span class="line">    <span class="keyword">if</span> c.timer.Stop() &#123;</span><br><span class="line">        <span class="comment">// 定时器未触发，释放资源</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这个里面还涉及了竞态处理：</p><ul><li><strong>竞态处理</strong> ：<code>Stop()</code> 返回布尔值，确保定时器未触发时才释放资源，避免 race condition。</li></ul><h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p><code>context</code> 的设计通过接口抽象还有组合模式实现了简洁高效的并发控制。<code>cancelCtx</code> 的递归取消和 <code>timerCtx</code> 的定时器机制共同实现了 Go 并发模型的核心能力。</p><p><del>当然了你如果不想用的话也是可以手动传递 channel 实现取消通知之类的方法啦。</del></p>]]></content>
    
    
      
      
    <summary type="html">&lt;p&gt;Go 的 &lt;code&gt;context&lt;/code&gt; 包是并发编程的核心工具，他的设计通过统一接口简化了 goroutine 的生命周期管理。其实核心就在于 &lt;code&gt;context.Context&lt;/code&gt; 接口的四个方法：&lt;code&gt;Deadline()&lt;/code</summary>
      
    
    
    
    
  </entry>
  
  <entry>
    <title>Go的连续栈</title>
    <link href="https://undefined.today/posts/cc4c72ab/"/>
    <id>https://undefined.today/posts/cc4c72ab/</id>
    <published>2024-11-13T01:12:22.000Z</published>
    <updated>2025-06-12T12:07:41.538Z</updated>
    
    <content type="html"><![CDATA[<p>连续栈这个概念是go 1.3版本引入的，它能通过动态调整栈空间的大小，解决传统固定栈或分段栈的缺陷。</p><h1 id="栈在go语言里面的演变"><a href="#栈在go语言里面的演变" class="headerlink" title="栈在go语言里面的演变"></a>栈在go语言里面的演变</h1><p>栈（Stack）是一种个遵循“后进先出”（当为在木桶里面装馕饼吃）原则的线性数据结构，主要来存储函数调用时的局部变量、参数和返回地址等信息。在传统编程语言中，栈通常被分配为固定大小的内存块，如果我们的程序递归过深或者是说局部变量过多，则可能导致<strong>栈溢出</strong> （Stack Overflow）。为解决这一问题，Go语言早期采用<strong>分段栈</strong> （Segmented Stacks）方案——每个goroutine初始分配8KB栈空间，当栈不足时通过链表扩展新的栈片段。然而，这种方案存在内存碎片化和频繁链表跳转的性能损耗。从Go 1.3起，Go引入了<strong>连续栈</strong>机制，通过动态迁移栈内容实现无缝扩容。</p><h1 id="核心机制"><a href="#核心机制" class="headerlink" title="核心机制"></a>核心机制</h1><h2 id="边界检测与溢出检测"><a href="#边界检测与溢出检测" class="headerlink" title="边界检测与溢出检测"></a>边界检测与溢出检测</h2><p>每个goroutine的结构体中维护着两个关键字段：<code>stackbase</code>（栈底地址）还有 <code>stackguard</code>（栈保护边界）。在函数调用的入口，编译器会插入汇编指令（如<code>CMPQ SP, g_stackguard</code>），把当前栈指针（SP）和<code>stackguard</code>比较。假设SP低于<code>stackguard</code>，那么触发了栈溢出检测。例如，以下伪代码展示了这一逻辑：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 函数调用时的栈检查（伪代码）</span></span><br><span class="line"><span class="keyword">if</span> SP &lt; g.stackguard &#123;</span><br><span class="line">    runtime.morestack()</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="扩容与缩容"><a href="#扩容与缩容" class="headerlink" title="扩容与缩容"></a>扩容与缩容</h2><ul><li><p><strong>扩容流程</strong> ：检测到栈溢出的时候，运行时会分配一块<strong>原栈两倍大小</strong>的新栈（例如从xKB扩展到2xKB），然后再把旧栈里面的数据完整复制到新栈里面去。然后，去更新goroutine的<code>stackbase</code>和<code>stackguard</code>让他俩指向新栈，最后跳转回原函数继续执行。</p></li><li><p><strong>缩容机制</strong> ：如果说一个goroutine长时间未使用大栈空间，运行时会在垃圾回收（GC）时检测空闲栈内存，并且把栈大小缩减至合理阈值（如恢复到xKB），来减少内存占用。</p></li></ul><h2 id="栈迁移与指针调整"><a href="#栈迁移与指针调整" class="headerlink" title="栈迁移与指针调整"></a><strong>栈迁移与指针调整</strong></h2><p>栈在扩容时，运行时需确保所有指向旧栈的指针依然可用。Go通过<strong>逃逸分析</strong> （Escape Analysis）追踪栈上的变量的生命周期，然后在迁移的时候更新所有活跃的指针的地址。这个过程需要依赖编译器的元数据记录变量在栈里面的偏移量。</p><h1 id="好在哪里"><a href="#好在哪里" class="headerlink" title="好在哪里"></a>好在哪里</h1><ul><li><p><strong>内存高效利用</strong> ：避免了分段栈的碎片化问题，栈空间按需分配而且是连续的，大大的提升内存利用率</p></li><li><p><strong>性能优化</strong> ：比起分段栈的链表跳转的话，连续栈迁移后函数调用无需额外跳转直接调用，降低间接寻址开销</p></li><li><p><strong>安全性增强</strong> ：栈保护边界（<code>stackguard</code>）还有硬件页的保护机制结合，能及时捕获栈溢出漏洞</p></li></ul><h1 id="坏在哪里"><a href="#坏在哪里" class="headerlink" title="坏在哪里"></a>坏在哪里</h1><p><del>没有缺点！</del></p><ul><li><p><strong>扩容成本</strong> ：栈复制依然需要消耗时间，特别是在频繁扩容场景下可能影响性能。所以，Go运行时通过<strong>预分配策略</strong> （如初始栈大小设为2KB而非8KB）来减少扩容次数</p></li><li><p><strong>指针调整复杂度</strong> ：栈迁移需精确更新所有的活跃的指针，这对编译器和运行时的协同提出了更高要求</p></li></ul>]]></content>
    
    
      
      
    <summary type="html">&lt;p&gt;连续栈这个概念是go 1.3版本引入的，它能通过动态调整栈空间的大小，解决传统固定栈或分段栈的缺陷。&lt;/p&gt;
&lt;h1 id=&quot;栈在go语言里面的演变&quot;&gt;&lt;a href=&quot;#栈在go语言里面的演变&quot; class=&quot;headerlink&quot; title=&quot;栈在go语言里面的演变&quot;</summary>
      
    
    
    
    
  </entry>
  
</feed>
